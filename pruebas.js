const {Rol,Usuario} = require('./models');

const roles = [{ nombre: "admin" }, { nombre: "user" }];

/*roles.forEach(rol => {
    Rol.build(rol).save().then(data => {
        console.log(data)
    });
});*/

